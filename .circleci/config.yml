version: 2.1
jobs:
  bobby:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: echo "A first hello"
  sally:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: echo "Double trouble"
workflows:
  version: 2.1
  bobby_sally:
    jobs:
      - bobby
      - sally
